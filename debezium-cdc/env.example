# Firebolt Configuration
# Get these from your Firebolt account settings
FIREBOLT_ACCOUNT=your-account-name
FIREBOLT_ENGINE_INGESTION=debezium_ingestion
FIREBOLT_ENGINE_ANALYTICS=debezium_analytics
FIREBOLT_DATABASE=demo_debezium_cdc
FIREBOLT_CLIENT_ID=your-client-id-here
FIREBOLT_CLIENT_SECRET=your-client-secret-here

# PostgreSQL Configuration (for Docker containers)
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin
POSTGRES_DB=postgres

# Kafka Configuration
KAFKA_BROKER=localhost:9092
ZOOKEEPER_HOST=localhost:2181

# Optional: Connector Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
KAFKA_CONNECT_URL=http://localhost:8083 